#pragma once

// ============================================================================
// Theme — 全局 UI 主题（阶段 12 GUI 美化）
// ============================================================================
//
// 深色现代主题（与日志面板深色协调，视频工具类应用主流风格）：
// - 全局字体 14px（解决默认 9pt 小字问题）
// - 主色 #3b82f6（蓝），状态色：进行中蓝 / 成功绿 / 失败红 / 取消灰
// - 深色面板 #1e1e2e / #252537 / #2a2a3c 三级背景
// - 圆角按钮 / 圆角输入框 / 高亮选中行
// ============================================================================

class QApplication;

namespace video_platform {

class Theme {
public:
    /// @brief 应用全局字体 + 样式表（main.cpp 调用一次）
    static void Apply(QApplication& app);
};

} // namespace video_platform
